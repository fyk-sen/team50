apiVersion: apps/v1
kind: Deployment
metadata:
  name: training-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: training-deployment
  template:
    metadata:
      labels:
        app: training-deployment
    spec:
      containers:
        - name: training-deployment
          image: training-deployment
          volumeMounts:
            - mountPath: "/data"
              name: shared-data
            - mountPath: "/mnt/model_storage"
              name: model_storage
      volumes:
        - name: shared-data
          emptyDir: {}
        - name: model_storage
          persistentVolumeClaim:
            claimName: model-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: training-service
spec:
  type: NodePort
  selector:
    app: training
  ports:
    - protocol: TCP
      port: 5002  
      targetPort: 5002